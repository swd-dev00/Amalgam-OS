# Static Site Configuration
# Note: Since this is a static build, these variables must be injected during the build process of the source repository.
VITE_APP_NAME=Amalgam Creator OS
VITE_API_URL=https://api.example.com
VITE_ENVIRONMENT=production
