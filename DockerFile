FROM node:20-alpine

WORKDIR /app

# Install simple HTTP server for serving static files
RUN npm install -g serve

# Copy static artifacts
COPY . .

# Expose port
EXPOSE 3000

# Start server
CMD ["serve", "-s", ".", "-l", "3000"]
